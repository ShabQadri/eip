services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: entertainment-intelligence-platform
    restart: unless-stopped
    volumes:
      - ./data:/app/data
    env_file:
      - .env
